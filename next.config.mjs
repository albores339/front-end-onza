/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['res.cloudinary.com', "img.icons8.com"], // Permitir imágenes de Cloudinary
    },
  };
  
  export default nextConfig;
  